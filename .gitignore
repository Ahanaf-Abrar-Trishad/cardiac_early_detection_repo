# Virtual environments
venv_wsl/
venv/
.venv/
env/

# Python cache
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# Model weights — tracked in git (all files are under 100 MB)
# *.pt intentionally not excluded

# Large external model formats (still exclude)
*.ckpt
*.bin

# Medical imaging data (use DVC or external storage for these)
*.nii.gz
*.nii
*.dcm
*.mhd
*.mha

# Generated images and previews — exclude bulk preview directories
logs/runs/
# Keep thesis figures (confusion matrices, segmentation examples) trackable
# by not globally excluding *.png — only exclude the bulk preview folder above

# Backup files
*.backup
*.bak

# Jupyter notebook checkpoints
.ipynb_checkpoints/

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
Thumbs.db

# Experiment tracking local artifacts
mlruns/
wandb/

# Build artifacts
*.egg-info/
dist/
build/
.claude/
