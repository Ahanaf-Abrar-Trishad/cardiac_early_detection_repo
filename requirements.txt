# === Core scientific stack ===
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.66

# === ML + CV ===
scikit-learn>=1.3
imbalanced-learn>=0.11
albumentations>=1.3
opencv-python-headless>=4.8
einops>=0.7
timm>=0.9
segmentation-models-pytorch>=0.3

# === Medical imaging ===
nibabel>=5.1
SimpleITK>=2.3
pydicom>=2.4
monai>=1.5
torchio>=0.19
scikit-image>=0.22
medpy>=0.5

# === Training utilities ===
pytorch-lightning>=2.3
hydra-core>=1.3
omegaconf>=2.3
optuna>=3.6

# === Experiment tracking (optional but recommended) ===
mlflow>=2.9
wandb>=0.17
rich>=13.7

# === Notebooks & dev ===
jupyterlab>=4.0
ipykernel>=6.25
black>=24.3
isort>=5.13
pytest>=7.4
pre-commit>=3.7
ruff>=0.6

# NOTE: PyTorch/torchvision must be installed FIRST with the correct CUDA wheel.
# Run this before `pip install -r requirements.txt`:
#
#   pip install torch==2.3.1 torchvision==0.18.1 --index-url https://download.pytorch.org/whl/cu121
#
# Adjust the CUDA suffix (cu121/cu118/cpu) to match your driver. See README for details.
# After finalising experiments, freeze exact versions with:
#   pip freeze > requirements_frozen.txt
